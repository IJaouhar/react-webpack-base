// import spinnerActions from './spinner.actions';
// import { SpinnerActionTypes, START, END, END_ALL } from './spinner.types';

// describe('Spinner actions', () => {
//     describe('start', () => {
//         it('should create an action to start spinner', () => {
//             const expectedAction: SpinnerActionTypes = {
//                 type: START,
//             };
//             expect(spinnerActions.start()).toEqual(expectedAction);
//         });
//     });

//     describe('end', () => {
//         it('should create an action to end spinner if count 1', () => {
//             const expectedAction: SpinnerActionTypes = {
//                 type: END,
//             };
//             expect(spinnerActions.end()).toEqual(expectedAction);
//         });
//     });

//     describe('endAll', () => {
//         it('should create an action to totally hide spinner', () => {
//             const expectedAction: SpinnerActionTypes = {
//                 type: END_ALL,
//             };
//             expect(spinnerActions.endAll()).toEqual(expectedAction);
//         });
//     });
// });
